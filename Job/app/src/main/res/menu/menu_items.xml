<menu xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto">
    <group android:checkableBehavior="single">
        <item
            android:id="@+id/nav_profile"
            android:icon="@drawable/ic_blog"
            android:title="profile" />

        <item
            android:id="@+id/nav_usertype"
            android:icon="@drawable/ic_interests"
            android:title="User Level" />
        <item
            android:id="@+id/nav_points"
            android:icon="@drawable/ic_interests"
            android:title="Points" />

        <item
            android:id="@+id/nav_invite"
            android:icon="@drawable/ic_interests"
            android:title="invite_users" />

    </group>
    <item android:title="config">
        <menu>
            <item
                android:id="@+id/nav_settings"
                android:icon="@drawable/ic_settings"
                android:title="settings" />
            <item
                android:id="@+id/nav_log_out"
                android:icon="@drawable/ic_log_out"
                android:title="log_out" />
        </menu>
    </item>
</menu>
